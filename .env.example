# Copy to .env and fill in. Never commit the real .env.

# Atlas connection string (Database > Connect > Drivers). Include the db user + password.
MONGO_URI=mongodb+srv://tahmidrockz00_db_user:X2XkaHQwU8Tcu1yq@ghostnode.6xoppsn.mongodb.net/?appName=ghostnode

# Logical database + collection the pings land in.
DB_NAME=playable_analytics
COLLECTION=events

# Port the HTTP server binds to.
PORT=3000
